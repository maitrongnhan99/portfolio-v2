{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ -n \"$VERCEL_GIT_PULL_REQUEST_ID\" ]; then exit 1; else exit 0; fi",
  "crons": [
    { "path": "/api/cron/qdrant-keepalive", "schedule": "0 3 * * *" }
  ]
}
